/*
 * Placeholder for vendored HTMX (~14 KB minified).
 *
 * The release artefact is not committed to the repo because the production
 * sandbox doesn't have outbound network. After cloning, run:
 *
 *   uv run python scripts/vendor_htmx.py
 *
 * to download htmx.min.js v1.9.12 and overwrite this file. Tests don't need
 * the real script — the FastAPI TestClient makes plain HTTP requests and
 * doesn't execute JS — so this placeholder is enough for CI.
 */
console.warn(
  "htmx.min.js is a placeholder. Run `python scripts/vendor_htmx.py` to vendor the real release."
);
